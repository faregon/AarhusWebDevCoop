@inherits Umbraco.Web.Macros.PartialViewMacroPage

@{
	var selection = CurrentPage.Site().FirstChild("articlesMain").Children("articleItem").Where("Visible").OrderBy("UpdateDate");
}
<ul>
	@foreach(var item in selection){
		<li>
			<a href="@item.Url">@item.Name</a>
		</li>
	}
</ul>

